server:
  port: 7071
  context-path: /brcrypto
management:
  port: 9071
endpoints:
  actuator:
    enabled: true
spring:
  datasource:
    url: jdbc:postgresql://javaleo.org:5433/postgres
    username: postgres
    password: gabro02
    continue-on-error: true
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
